

function trocarimagem() {
    const imgElement = document.getElementById('imagem');
    const currentSrc = imgElement.getAttribute('src');

    if (currentSrc === '../imgs/Favorite.png') {
        imgElement.setAttribute('src', '../imgs/Love.png');
        
    } else {
        imgElement.setAttribute('src', '../imgs/Favorite.png');
        
    }    
        }

        function trocarimagem2() {
    const imgElement = document.getElementById('imagem2');
    const currentSrc = imgElement.getAttribute('src');

    if (currentSrc === '../imgs/Read.png') {
        imgElement.setAttribute('src', '../imgs/Bookmark.png');
    } else {
        imgElement.setAttribute('src', '../imgs/Read.png');
    }
}



function trocarimagem3() {
    const imgElement = document.getElementById('imagem3');
    const currentSrc = imgElement.getAttribute('src');

    if (currentSrc === '../imgs/Favorite.png') {
        imgElement.setAttribute('src', '../imgs/Love.png');
    } else {
        imgElement.setAttribute('src', '../imgs/Favorite.png');
    }    
        }

        function trocarimagem4() {
    const imgElement = document.getElementById('imagem4');
    const currentSrc = imgElement.getAttribute('src');

    if (currentSrc === '../imgs/Read.png') {
        imgElement.setAttribute('src', '../imgs/Bookmark.png');
    } else {
        imgElement.setAttribute('src', '../imgs/Read.png');
    }
}



function trocarimagem5() {
    const imgElement = document.getElementById('imagem5');
    const currentSrc = imgElement.getAttribute('src');

    if (currentSrc === '../imgs/Favorite.png') {
        imgElement.setAttribute('src', '../imgs/Love.png');
    } else {
        imgElement.setAttribute('src', '../imgs/Favorite.png');
    }    
        }

        function trocarimagem6() {
    const imgElement = document.getElementById('imagem6');
    const currentSrc = imgElement.getAttribute('src');

    if (currentSrc === '../imgs/Read.png') {
        imgElement.setAttribute('src', '../imgs/Bookmark.png');
    } else {
        imgElement.setAttribute('src', '../imgs/Read.png');
    }
}


function trocarimagem7() {
    const imgElement = document.getElementById('imagem7');
    const currentSrc = imgElement.getAttribute('src');

    if (currentSrc === '../imgs/Favorite.png') {
        imgElement.setAttribute('src', '../imgs/Love.png');
    } else {
        imgElement.setAttribute('src', '../imgs/Favorite.png');
    }    
        }

        function trocarimagem8() {
    const imgElement = document.getElementById('imagem8');
    const currentSrc = imgElement.getAttribute('src');

    if (currentSrc === '../imgs/Read.png') {
        imgElement.setAttribute('src', '../imgs/Bookmark.png');
    } else {
        imgElement.setAttribute('src', '../imgs/Read.png');
    }
}


function trocarimagem9() {
    const imgElement = document.getElementById('imagem9');
    const currentSrc = imgElement.getAttribute('src');

    if (currentSrc === '../imgs/Favorite.png') {
        imgElement.setAttribute('src', '../imgs/Love.png');
    } else {
        imgElement.setAttribute('src', '../imgs/Favorite.png');
    }    
        }

        function trocarimagem10() {
    const imgElement = document.getElementById('imagem10');
    const currentSrc = imgElement.getAttribute('src');

    if (currentSrc === '../imgs/Read.png') {
        imgElement.setAttribute('src', '../imgs/Bookmark.png');
    } else {
        imgElement.setAttribute('src', '../imgs/Read.png');
    }
}




function trocarimagem11() {
    const imgElement = document.getElementById('imagem11');
    const currentSrc = imgElement.getAttribute('src');

    if (currentSrc === '../imgs/Favorite.png') {
        imgElement.setAttribute('src', '../imgs/Love.png');
    } else {
        imgElement.setAttribute('src', '../imgs/Favorite.png');
    }    
        }

        function trocarimagem12() {
    const imgElement = document.getElementById('imagem12');
    const currentSrc = imgElement.getAttribute('src');

    if (currentSrc === '../imgs/Read.png') {
        imgElement.setAttribute('src', '../imgs/Bookmark.png');
    } else {
        imgElement.setAttribute('src', '../imgs/Read.png');
    }
}

function trocarimagem13() {
    const imgElement = document.getElementById('imagem13');
    const currentSrc = imgElement.getAttribute('src');

    if (currentSrc === '../imgs/Broken Heart2.png') {
        imgElement.setAttribute('src', '../imgs/Broken Heart.png');
    } else {
        imgElement.setAttribute('src', '../imgs/Broken Heart2.png');
    }
}

function trocarimagem14() {
    const imgElement = document.getElementById('imagem14');
    const currentSrc = imgElement.getAttribute('src');

    if (currentSrc === '../imgs/Broken Heart2.png') {
        imgElement.setAttribute('src', '../imgs/Broken Heart.png');
    } else {
        imgElement.setAttribute('src', '../imgs/Broken Heart2.png');
    }
}

function trocarimagem15() {
    const imgElement = document.getElementById('imagem15');
    const currentSrc = imgElement.getAttribute('src');

    if (currentSrc === '../imgs/Broken Heart2.png') {
        imgElement.setAttribute('src', '../imgs/Broken Heart.png');
    } else {
        imgElement.setAttribute('src', '../imgs/Broken Heart2.png');
    }
}

function trocarimagem16() {
    const imgElement = document.getElementById('imagem16');
    const currentSrc = imgElement.getAttribute('src');

    if (currentSrc === '../imgs/Broken Heart2.png') {
        imgElement.setAttribute('src', '../imgs/Broken Heart.png');
    } else {
        imgElement.setAttribute('src', '../imgs/Broken Heart2.png');
    }
}

function trocarimagem17() {
    const imgElement = document.getElementById('imagem17');
    const currentSrc = imgElement.getAttribute('src');

    if (currentSrc === '../imgs/Broken Heart2.png') {
        imgElement.setAttribute('src', '../imgs/Broken Heart.png');
    } else {
        imgElement.setAttribute('src', '../imgs/Broken Heart2.png');
    }
}

function trocarimagem18() {
    const imgElement = document.getElementById('imagem18');
    const currentSrc = imgElement.getAttribute('src');

    if (currentSrc === '../imgs/Broken Heart2.png') {
        imgElement.setAttribute('src', '../imgs/Broken Heart.png');
    } else {
        imgElement.setAttribute('src', '../imgs/Broken Heart2.png');
    }
}


